from JParse.Entry import Entry

def lookup(e):
    """
    Looks up entry in dictionary and populates entry's data
    :param e: an object of class Entry
    :return: nothing - modifies passed entry and fills in parameters with info from lookup
    """
    # Connect to DB
    # Get entry
    # Parse into variables
    # Check if furigana exists, or look it up if needed. Either way, compare
    if not self.furigana:
        pass